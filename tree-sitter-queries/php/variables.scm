(expression_statement
  (assignment_expression
    left: (variable_name
      (name) @variable
)))

(formal_parameters
  (simple_parameter
    name: (variable_name
      (name) @variable
)))

(property_declaration
  (property_element
    (variable_name
      (name) @variable
)))

