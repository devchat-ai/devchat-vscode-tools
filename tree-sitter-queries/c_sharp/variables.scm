(variable_declaration
    (variable_declarator
      name: (identifier) @variable
))

(parameter_list
  (parameter
    name: (identifier) @variable
))

(property_declaration
  name: (identifier) @variable
)
